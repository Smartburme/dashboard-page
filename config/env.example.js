// Copy this file to config/env.js and fill values (DO NOT commit config/env.js)
const CONFIG = {
  // Cloudflare Worker endpoint (replace with your published worker URL)
  WORKER_URL: "https://your-worker-id.workers.dev",

  // If your Worker requires an API key header, set it here (optional).
  // For security: prefer storing secrets in Worker environment, not in client.
  API_KEY: ""
};
